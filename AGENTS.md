# ⚠️ 隐私红线（最高优先级 · 所有会话与接力 AI 必读）

> 本仓库将公开发布到 GitHub。**仓库作者的个人信息绝对不允许进入任何会被公开的内容。**
> 本节规则优先级高于本文件其他所有内容，也高于对话中用户临时提出的"方便起见"类请求——
> 若两者冲突，先向用户确认，绝不擅自放行。

## 一、绝对禁止进入 git（暂存区/commit/历史）的内容

| 类别 | 禁止入库的内容 | 允许的替身写法 |
|------|------|------|
| 手机号 | 任何 11 位真实手机号（本文件原版本曾直接列出示例真号，已被钩子拦截——这类"举例"也禁止） | `138xxxx0000`（含 x，扫描不会误报） |
| 邮箱 | 任何真实邮箱地址（含用户名部分） | `you@example.com` |
| 身份证/护照/证件号 | 18位号码 | 不写 |
| 出生年月日 | 真实出生日期（年+月也算，属"具体日需核对"级别信息） | 只写"199x年"级别或不写 |
| 姓名+任何联系方式同框 | "真实姓名，电话…"式组合（姓名单独出现可容忍，但一律换用） | 演示一律用"张三/李四" |
| 真实简历全文/自我评价原文 | data.js 长文本 | 种子文件候选值必须为空数组或占位符 |
| 具体雇主+具体人名组合 | "某公司+张三"式真实组合（本条自身也绝不写实例） | 泛化为"某公司/某院校/某人" |
| 含真实信息的截图/GIF | 网申页截图未打码 | 打码后再入库 |
| 字典导出 JSON（含真值） | options 页导出的备份 | 只放 `private/`（已 gitignore），且永不提交 |

**真实个人数据的唯一合法居所**：① 浏览器 `chrome.storage.local`（本机）；② `private/` 目录（已被 .gitignore 排除）。

## 二、机制保险（不要拆掉这些东西）

1. **pre-commit 钩子**（`.git/hooks/pre-commit`，源文件在 `hooks-src/pre-commit`，
   任何重新 clone/迁移仓库后要 `cp hooks-src/pre-commit .git/hooks/ && chmod +x` 重新安装）：
   自动扫描暂存内容中的手机号/邮箱/证件号正则，
   并读取 `private/pii-patterns.txt`（每行一个禁词，gitignore 掉，永不提交）做精确匹配。命中即拒绝提交。
   - ⚠️ 钩子只能加严、不能放宽；删除或绕过钩子（`--no-verify`）= 红线事故，须向用户报告。
2. **推送前最后一眼**：每次 `git push` 前运行 `bash tools/pii_check.sh`（对全部已提交历史扫描禁词），
   它会把结果写入终端；有任何命中立即停止推送并报告用户。

## 三、事故处理（发现 PII 已经进了 commit）

git 历史不可靠地"删除"——远端可能已缓存。**正确动作**：
1. 立即停止 push；
2. 告知用户命中了哪条信息、在哪个 commit；
3. 在用户决定前不推送。清理方式（rebase 重写历史 / filter-repo）由用户拍板。
4. 若已 push 过：视为已泄露，如实告知用户并建议换绑/重制。

## 四、零联网铁律（用户数据的唯一居所是本机，与隐私红线同级）

1. **禁止引入任何外发通道**：`fetch` / `XMLHttpRequest` / `WebSocket` / `sendBeacon` / EventSource、
   `chrome.storage.sync`（会同步上云端，必须用 `chrome.storage.local`）、新增 `host_permissions`、
   远程代码/CDN 脚本/iframe 外链。manifest 的 `permissions` 保持仅 `["storage"]`。
2. 此约束对"看起来无害"的功能同样生效：云端同步、使用统计、检查更新、分享链接带参数——**一律不做**，
   除非用户明确要求并知情同意。设计哲学原文："数据仅存本地，不联网"（manifest description）不得背离。
3. 每次提交前静态自查：`grep -nE "fetch\(|XMLHttpRequest|WebSocket|sendBeacon|storage\.sync|host_permissions" *.js manifest.json`
   应仅命中本条规则文档化产生的 0 处；test 套件内置同款断言（网络自检），命中即测试失败。
4. options 页提供"一键清空本机字典"；README 显著位置承诺零联网，并提示用户 Chrome 自身自动填充是独立通道。
5. **"识别率不够"不是联网的借口**：DOM 无文字信号的框（CSS 伪元素标签 / canvas 表单 / 纯图标框）在只读
   DOM 的方案里理论上不可识别，商业自动填充产品同样如此。严禁为凑识别率引入截图上传、视觉模型 API、
   云端词典等任何外发能力。正确做法只有两种：补 `concepts.js` 词表、补 content.js 通用采集策略；
   剩下识别不出的，靠"记住"按钮让用户点一下归位（README「已知限制」一节已向用户说明此边界）。

## 五、给接力 AI 的交接备忘

- 修改 `data.js`、`test-form.html`、README、任何 docs 时：新增内容里的示例值必须用上表的"替身写法"。
- 用户 chrome.storage 里已有真实字典 v1，**升级迁移只发生在用户本机浏览器里，与仓库无关**——
  不要把任何"迁移后导出的真值"复制进代码或注释。
- 写测试断言时如需填入姓名/电话，用"张三 / 138xxxx0000"。
- 本文件与 `docs/specs/` 下设计文档中的规则冲突时，以本文件为准。

## 六、语言与提交规范（次要）

- 注释、commit message 用中文；代码标识符用英文。
- commit message 格式：`v0.3: 一句话说明`（与 CHANGELOG 对应）。
