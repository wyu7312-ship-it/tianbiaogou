# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格。
所有演示数据均为假名，仓库不含任何使用者个人信息。

## v0.4 — 2026-09

### 变更（改名）
- **项目名 `网申填充助手` → `填表狗`**（仓库同步改名 `wsa-autofill` → `tianbiaogou`，GitHub 会自动重定向旧地址）。
  扩展显示名（`manifest.json` 的 `name`）、选项页标题、各源码文件头注释一并更新。
- README 重写：开头讲清楚"为什么有这东西"（毕业狗秋招填表填到麻），
  正文按"怎么做的"分五节讲实现思路（点选只填当前框 / 概念表归位 / 人称 scope / 记住兜底 / 零联网）。

### 修复（标签识别 —— 卡片标题取不到真标签）
- **旁支兄弟含 select/hidden 被误杀**：跳过条件原本是递归的 `querySelector('input,textarea,select')`，
  导致 `<div class="label">所学专业<select>…</select></div>` 这种"专业/学校用下拉选"的主流形态
  整块丢标签（标题退化成裸 id）。现在只把**可输入文本的可见控件**当作"另一个字段"，
  `hidden`/`disabled`/不可见/`select` 都不算。
- **标签采集改为带可信度打分**：`label[for]`/`closest(label)` > 旁支兄弟 > `td` 前兄弟 >
  `placeholder` > 前兄弟 > 祖先文本。修掉"校验文案 `邮箱格式不正确` 抢在真标签 `电子邮箱` 前面"。
- **长度上限 12 → 24 字**，并剥掉尾部 `必填/选填` 标记（`紧急联系人电话必填` → `紧急联系人电话`）。
- **占位符剥壳**：`请输入验证码` → `验证码`；纯 `请输入/请选择` 仍判为无信息。
- **裸 id 不再当标题**：取不到中文标签时显示「未识别」，不再出现 `a4`/`field_2384`。
- 新增 `HINT_CLASS` / `looksLikeValidation` / `isNoise` 剔除告警、校验、计数类文案。

### 修复（写入诚实性）
- `setNativeValue` 原本无条件返回 `true`，"已填入"可能是假成功（被 `maxlength` 截断、
  被格式化、被框架还原都报成功）。现在写入后**回读核对**，分三态提示：
  已填入 / 被页面改写为「X」（列出原文）/ 未生效请手动输入。
- `select` 部分匹配命中多项时不再静默选第一个，改为照填并在提示里**列出全部相近选项**请用户核对。

### 修复（安全性 —— 过度匹配）
- 低分概念只是"高分概念匹配词的子串"时抑制其候选（`号码 ⊂ 身份证号码`）。
  原本「紧急联系人电话」框在没存过紧急联系人时，会**列出本人手机号**，极易误填。
  反向保护：`联系电话` 的 `电话` 正等于整串，仍正常命中（单测 T8 双向守住）。

### 修复（数据质量）
- `remember()` 改为"读改写"（`get → 合并 → clean → set`），修掉与「字典管理」页并发保存互相覆盖。
- `cleanDict` 提到 `matcher.js` 由 `content.js` / `options.js` 共用，两条写入路径产出一致。
- 标签识别失败时**拒绝入库**，不再存成 `custom: "a4"` 这类垃圾键（永久污染且再也匹配不上）。
- 命中概念但本机没存过值时，提示改为"已识别为「X」，但本机还没存过这类值"。

### 测试
- `tests/labels.html`（新增）：标签 15 例 + 应未识别 2 例 + 写入 4 例 + 危险候选 4 例 + 垃圾键 1 例 → 28/28。
- `tests/unit.html`：新增 T8（过度匹配抑制/反抑制）、T9（cleanDict 契约）→ 66/66。
- `tests/integration.html` 20/20。
- `tests/harness_shell.html` + `tests/harness.js`（新增）：隔离 harness，
  免重启浏览器即可验证 `content.js` 改动（srcdoc iframe 避开已装扩展注入）。

### 变更
- host 版本探针 `data-wsa` 0.3 → **0.4**；host id 支持 `documentElement.dataset.wsaHostId` 覆盖（测试隔离用）。

## v0.3 — 2026-09-17

### 新增
- **概念表匹配**：`concepts.js` 中英同义词自动归位——"移动号码 / Contact Number / 联系电话"等都归到同一「手机号」概念，无需逐个记忆。
- **人称 scope**：一个「姓名」概念可并列存本人 / 母亲 / 父亲 / 紧急联系人，卡片全列带标签由你点选，不再互相覆盖。
- **三级匹配**：同义词包含 → 人称限定词识别 → 二字滑片弱回退（卡片前缀 `≈`）。
- **存储 v2 + 自动迁移**：旧版碎标签字典首次启动自动按概念归位，保留 `wsa_dict_backup_v1` 可回滚。
- **首启初始化向导**：第一次打开引导用户自填一遍（敏感项标"选填·只存本机"），可跳过边用边记。
- **一键清空本机字典**（两次确认）；导入 / 导出升级到 v2。
- **记忆入口常驻**：有候选时也能追加值 / scope，修掉"母亲姓名无法添加"。

### 变更
- 记住改为"自动归位"：命中概念则并入其候选（新叫法进别名，带限定词的打 scope），命中不了才独立成自定义字段。
- 数据出厂置空（`data.js` 仅占位），仓库与测试全部改用假名。

### 安全 / 隐私
- 零联网铁律 + 自检单测（源码扫描 `fetch/XHR/WebSocket/sendBeacon/storage.sync/host_permissions`、manifest 权限仅 `storage`）。
- git pre-commit PII 钩子 + 全历史 `tools/pii_check.sh`；`AGENTS.md` 面向后续协作者固化隐私红线。

### 测试
- 逻辑层 `tests/unit.html` 50/50；端到端集成 `tests/integration.html`（chrome.storage 垫片驱动真实 `content.js`）20/20。

### 修复（同日实测热修）
- **zhiye/北森系"姓名变请输入"漏采**：这类 ATS 把标签做成与控件容器并列的兄弟元素（input 自身无 id/name/aria/紧邻兄弟），旧版沿祖先链只读直接文本节点，整条标签漏抓、卡片标题只剩占位符"请输入"。现沿祖先链采集"路径元素的前兄弟"文字，就近命中即停，且**跳过自身含输入控件的兄弟**（那是相邻字段，防止串标签）——已在嘉兴银行真实页面只读探针验证识别为「姓名」。
- **占位符冒充标签**：卡片标题过滤"请输入 / 请选择 / 填写内容 / 必填"等无信息提示语，有真实标签绝不用提示语兜底。

## v0.2 — 2026-09-11

### 新增
- 弹卡默认定位改到**输入框右侧**（表单选项常在框下方，右置不挡视线），空间不足依次退到下 / 左 / 上。
- 按住标题栏**拖动移位**且跨字段保持手动位置；双击标题栏恢复自动。

## v0.1 — 2026-09-10

### 新增
- MVP：点输入框弹候选卡片，点选 / `↑↓`+回车**只填当前框**；native setter + `input/change` 事件写入，兼容 React/Vue 受控组件。
- 按 label 匹配的本地字典 + "记住新字段"增量积累；字典管理页（编辑 / 导入导出 JSON）。
- 13/13 自动化测试通过。
