# 真实个人数据目录 —— 永不提交（AGENTS.md 隐私红线）
private/

# 浏览器/系统杂物
Thumbs.db
desktop.ini
*.swp

# 导出备份若落在仓库根目录也一律排除
*.wsa-dict.json

# ---------------------------------------------------------------------------
# 调试探针与 dump —— 永不提交（真值常从浏览器存储原样带出）
#
# ⚠️ 两条硬约束（2026-09 实测踩坑）：
#  1) Chrome 拒绝加载"目录内含文件名以 _ 开头"的未打包扩展，
#     报错：Filenames starting with "_" are reserved for use by the system。
#     所以仓库**根目录绝不能出现 _ 开头的文件**，否则扩展根本装不上。
#  2) 探针 dump 直接来自浏览器存储，常带真实个人信息。
#
# 因此：探针脚本与 dump 一律放 tools/probe/（子目录不影响 Chrome 加载），
#       根目录不再使用 _* 命名。
# ---------------------------------------------------------------------------
tools/probe/dump/

# 兼容历史写法：万一根目录又出现 _ 开头文件，兜住不让它进仓库
# （注意：这只能防提交，不能防 Chrome 拒载——所以根本办法是别在根目录建这类文件）
/_*
